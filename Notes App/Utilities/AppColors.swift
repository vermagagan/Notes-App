import UIKit

enum AppColors {
    
    // MARK: - Brand
    static let primary = UIColor(red: 0.98, green: 0.76, blue: 0.22, alpha: 1.0)
    // Warm Yellow — buttons, icons, accents #F9C238
    
    static let primaryDark = UIColor(red: 0.85, green: 0.63, blue: 0.10, alpha: 1.0)
    // Darker Yellow — pressed/active state #D9A119
    
    // MARK: - Text
    static let header = UIColor(red: 0.13, green: 0.13, blue: 0.13, alpha: 1.0)
    // Near Black — titles, main text #212121
    
    static let subtitle = UIColor(red: 0.55, green: 0.55, blue: 0.55, alpha: 1.0)
    // Medium Gray — preview text, secondary info #8C8C8C
    
    static let placeholder = UIColor(red: 0.75, green: 0.75, blue: 0.75, alpha: 1.0)
    // Light Gray — placeholders, dates #BFBFBF
    
    // MARK: - Background
    static let background = UIColor(red: 0.98, green: 0.97, blue: 0.95, alpha: 1.0)
    // Warm Off-White — all screen backgrounds #FAF8F2
    
    static let cardBackground = UIColor.white
    // Pure White — note cells / cards #FFFFFF
    
    static let secondaryBackground = UIColor(red: 0.94, green: 0.93, blue: 0.91, alpha: 1.0)
    // Slightly Darker Warm White — search bar, input fields #F0EEE8
    
    // MARK: - Semantic
    static let destructive = UIColor(red: 0.95, green: 0.33, blue: 0.33, alpha: 1.0)
    // Soft Red — swipe to delete #F25454
    
    static let divider = UIColor(red: 0.90, green: 0.89, blue: 0.87, alpha: 1.0)
    // Warm Light Gray — separators, lines #E6E4DF
}
